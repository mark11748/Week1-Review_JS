yearToSeconds(age) {
  // return (age*(365*24*60*60));
};
mercuryAge(age) {
  // return age*.24;
};
venusAge(age) {
  // return age*.62;
};
marsAge(age) {
  // return age*1.88;
};
jupiterAge(age) {
  // return age*11.86;
};
getLifeExpectancy(age) {
  // let currentYr = new Date.getFullYear();
  // if((currentYr-age)<2000){return 70;}
  // else {return 75;}
};
